

window.addEventListener("load", function(){
    var payBox = this.document.querySelector(".pay-box");
    var payPop = this.document.querySelector(".pay-pop");

    payBox.onclick = function(e) {
        e.preventDefault();
    };

});